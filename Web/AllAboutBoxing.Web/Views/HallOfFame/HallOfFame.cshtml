@model AllAboutBoxing.Web.ViewModels.HallOfFame.HallOfFameBoxersListViewModel
@{
    this.ViewData["Title"] = "The greatest boxers of all time";
}

<h1 class="text-center">@this.ViewData["Title"]</h1>

<hr />
<p>Mike Tyson, Joe Frazier and of course Muhammad Ali — there have been many legendary heavyweight boxers. Here's our list of the very best.</p>
<hr />

@foreach (var boxer in Model.HallOfFameBoxers)
{
    <div class="text-center list-group-item-dark">
        <div class="teaserImg">
            <img class="img-thumbnail" src="@boxer.BoxerImageUrl">
        </div>
        <div class="teaserContentWrap">
            <div class="tools">
            </div>
            <h2>@boxer.BoxerName</h2>
            <p>@boxer.Description</p>
        </div>
    </div>
}

<nav aria-label="...">
    <ul class="pagination justify-content-center">
        <li class="page-item @if (!Model.HasPreviousPage)  { <text>disabled</text> }">
            <a class="page-link" asp-action="HallOfFame" asp-route-id="@Model.PreviousPageNumber">Previous</a>
        </li>
        @for (int i = Model.PageNumber - 2; i < Model.PageNumber; i++)
        {
            @if (i > 0)
            {
                <li class="page-item"><a class="page-link" asp-action="HallOfFame" asp-route-id="@i">@i</a></li>
            }
        }

        <li class="page-item active">
            <span class="page-link">
                @Model.PageNumber
                <span class="sr-only">(current)</span>
            </span>
        </li>

        @for (int i = Model.PageNumber + 1; i <= Model.PageNumber + 2; i++)
        {
            @if (i <= Model.PagesCount)
            {
                <li class="page-item"><a class="page-link" asp-action="HallOfFame" asp-route-id="@i">@i</a></li>
            }
        }
        <li class="page-item @if (!Model.HasNextPage)  { <text>disabled</text> }">
            <a class="page-link" asp-action="HallOfFame" asp-route-id="@Model.NextPageNumber">Next</a>
        </li>
    </ul>
</nav>

